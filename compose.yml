services:
  postgres-db:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: ${DB_USERNAME}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: plantdiarydb
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - plantdiary-network

  backend:
    image: ${DOCKER_USERNAME}/plantdiary-backend:${BRANCH_NAME}-latest
    ports:
      - "8080:8080"
    depends_on:
      - postgres-db
    environment:
        DB_USERNAME: ${DB_USERNAME}
        DB_PASSWORD: ${DB_PASSWORD}
    networks:
     - plantdiary-network

  frontend:
    image: ${DOCKER_USERNAME}/plantdiary-frontend:${BRANCH_NAME}-latest
    ports:
      - "5173:80"
    depends_on:
      - backend
    networks:
        - plantdiary-network

volumes:
  postgres-data:

networks:
  plantdiary-network: